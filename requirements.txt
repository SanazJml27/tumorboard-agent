# Core dependencies -- everything needed to run the pipeline fully offline.
pydantic>=2.0
numpy>=1.24
scikit-learn>=1.3
pytest>=7.4
streamlit>=1.35

# --- Optional extras ---
#
# To use the in-app "Rationale phrasing (LLM)" backends (paste a key in the
# sidebar, or set the env var), install the matching SDK. Decisions stay
# deterministic regardless; these only affect prose wording:
#   anthropic>=0.34   # enables the Anthropic (Claude) backend
#   openai>=1.30      # enables the OpenAI (GPT) backend
#
# Regenerating the figures in docs/ (scripts/render_figures.py):
#   cairosvg>=2.7
